package com.chitu.bigdata.sdp.api.enums;

public enum RunningEnv {
    /*DEV("dev"),
    UAT("uat"),
    PRO("prod");
    private String type;
    RunningEnv (String type){this.type = type;}
    public String getType() {
        return type;
    }

    public void setType(String type) {
        this.type = type;
    }

    public static String ofType(String type){
        for (RunningEnv value : values()) {
            if (value.getType().equalsIgnoreCase(type)){
                if (DEV.getType().equalsIgnoreCase(type)){
                    return UAT.getType().toUpperCase();
                }
                return value.getType().toUpperCase();
            }
        }
        return null;
    }*/
}
